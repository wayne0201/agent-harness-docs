# 《深入浅出 AI Agent》写作规范

> 本书的「写作宪法」。所有章节的撰写与修改必须遵循本文件；每次根据读者反馈调整后，把新共识同步进本文件，再继续写下一章。
>
> **谁读**：本文件每次会话自动注入给写作 Agent——这是本书项目自己的 AGENTS.md，属于「人记」一层的规矩。**何时读**：动笔写 / 改任何章节前，按 §1–§9 逐节对照；改完一章，把普适规律写进 §5、意见写进 §10。
>
> **核心问题**：怎么写出一本既深又浅的书——机制讲透、读者接得住？
>
> **宪法口诀**：正文存事实，宪法存规律；规律不过二十，台账不超二十；做完一章，再走下一章。

## 1. 定位与主线

- 书名：《深入浅出 AI Agent》；范例：DeepSeek Harness（`dsh`）。读者假设：有编程基础，不要求懂任何特定框架；重「心智模型」，轻 API 手册。具体章序与目录以 `outline.html` 大纲为准（宪法不重复大纲）。
- **主线**：事物发展遵循 **哲学 → 科学 → 艺术**，三篇一一对应——**先问为什么**（世界观与第一性原理）→ **建立机制**（解剖 dsh 的插件/事件/接缝/循环/日志）→ **长出品**（工具与提示设计、调试、评估、全链路闭环）。
- **全书心智模型**（第 1 章确立，反复回扣）：**Agent = 模型（思考）+ 记忆（上下文）+ 工具（行动），由循环（Loop）驱动**。
- **暗线**：全链路工程师（发现 → 梳理 → 落地 → 反馈 的闭环）。
- **双线结构（硬性）**：贯穿两条线——**应用线**（用好 Agent）与**创造线**（构建 Agent）；有边界、不设墙，总原则「在用中做、在做中用」。每章标四类标签之一：**共用 / 应用 / 创造 / 结合**。
  - **判定口诀（硬性）**：双线看主语——改 Agent「体内」→ 创造线；改你与 Agent 的「相处」→ 应用线；双方共同底子 → 共用。快速一问：「这条知识改的是体内，还是你们之间的约定？」
  - 编织法：主题成对相邻；应用章结尾埋「做」的钩子，创造章回看「用」的体验。
- **同一标本两种眼光**：第 7 章（应用）讲 AI 工程化（dsh 怎么用 AI 养自己的项目）；第 13 章（创造）讲代码架构（dsh 作为一个 Agent 怎么拼起来）。
- **章号引用规则（重编号时必查）**：指向「篇」的引用不变；篇内局部章号与图表裸数字引用（如「应用线 · 17」）必须随重编号同步（曾把「第二篇第 5、6 章」修正为新第 8、9 章）。
- **三篇分工（硬性）**：哲学篇只讲<b>概念与领域知识</b>（抽象、类比、思想史），不深入机制与源码；机制细节（源码、流程走查、包名实现）一律后移到科学篇（优先抽象化重写，其次迁入对应科学篇章节）。

## 2. 每章统一体例

按以下顺序组织一章（后续章节全部沿用）：

1. **导语**：hero（章号 + 标题 + 一句话副题）+ lead 段 + 「本章核心问题」callout（idea 类型）。
2. **本章导览**：card 内的小节锚点链接列表。
3. **正文小节**：h2 编号（1.1、1.2…），小节内是「短段落 + 图/表/列表/callout」的组合。
4. **一张图总结**：把全章脉络收拢成一张图（参考第 1 章图 1-6 的三步式）。
5. **要点回顾**：3–5 条，用列表，每条一句话。
6. **动手做**：不依赖代码的小练习，1–3 步，写在 card 里。

每章（含占位 stub）还需保留三段元信息：**核心问题**（解决什么）、**本章结构**（节级列表）、**你将获得**（读完得到什么）。
- 双线归属靠章徽章与正文自然表达，不单设「双线地图」卡（读者反馈：生硬，应融入内容）；只有跨界知识点才挂**双线提示** callout（class="callout dual"，标题「⇄ 双线提示」，写明另一条线的视角）。
- 全章核心口径 / 口诀句（如「AGENTS.md 每次必读 · …」四种吃法）用 callout 高亮承载（note 类型 + 短标题），不埋在段落里只加粗——读者点名要高亮第 10 章口诀后确立。

## 3. 可读性要求（最高优先级）

- **段落短**：1–3 句；禁止一大段文字堆砌。
- **每小节至少一个非纯文字元素**：图、表、列表或 callout。
- 关键概念首次出现加粗（`<b>`）；重点句可整句加粗，但一屏内不超过两三处。
- 中文引号用「」；中英文之间留空格（如「Agent 工程师」）。
- 结构感优先：列表、表格、callout 优于长句；能用一句话讲清的不写一段。
- **中文表述优先**：概念分组避免直译词（如「侧面 N」），优先用动词式标题（如「怎么拼：时空可组合性」）；能用日常说法就不造词。**章标题同理**：判断句、名词化读起来不通顺时，改用动词式·生活化标题（曾把《好项目是「AI 友好」的项目》改为《让项目会「自我介绍」》）。
- **并列实体必分点（硬性）**：一个段落里并列三个以上实体（如 README / docs/ / 子树 AGENTS.md）必须拆成列表或表格，每项一句话；抽象法则也拆成「一句话 + 实例分点 + 对照表」，不得用大段罗列。
- **疑问融入正文（硬性）**：常见疑问不要用「一问一答」式小节呈现（突兀），把答案织进正文叙述——在流程讲解处直接讲清（如路径链、用到才读的口径写进交接流程本身）。
- **写作从简（硬性）**：可有可无的解释性括注、自我夸赞式铺垫（如「文档比假想项目还多」）直接删除，不画蛇添足。

## 4. 插图规范（内联 SVG）

- 只用**内联 SVG**，不依赖外部资源，离线可看；不引入图表库 CDN。
- **图编号**：图 章-序（图 1-1、1-2…）；子图：图 1-2a。
- **每图必须配 figcaption**：`<b>图 X-Y</b> + 一句话说明`。
- **箭头必有落点（硬性）**：每条带 marker 的 line / path，终点必须落在某个元素（盒子或文本）范围内，不许指向空白处；删掉某个目标盒时同步删掉/改指它的箭头。第 10 章图 10-3 曾有一条从备菜台指向空白处的残箭（旧稿写回盒已删、箭头未删），读者发现后删除，箭头数才与 caption「四条通道」一致。
- **边界检查（硬性）**：所有 rect/text 必须落在 viewBox 与所属面板边界内；曾因「输出」盒右缘 354 > 面板 346 返工，写完必查最大/最小坐标。
- **元素组居中（硬性）**：面板/画布内的元素组，其组中心应与面板/画布中心对齐（曾因「积木式」面板元素组整体偏左 44px 返工，写完必算组中心）。
- **marker id 全页唯一**（m11、m12、m12a、m15c…）：SVG 的 id 是文档全局的，重复会互相覆盖箭头颜色。
- SVG 内 `<g>` 统一加 `font-family='-apple-system,"PingFang SC","Microsoft YaHei",sans-serif'`。
- **配色（全书统一）**：
  - 哲学/模型类：靛蓝 `#4338ca`，底色 `#eef2ff`，描边 `#c7d2fe`
  - 科学/循环类：青绿 `#0f766e`，底色 `#ecfdf8`，描边 `#5eead4`
  - 艺术/行动类：琥珀 `#b45309`，底色 `#fffbeb`，描边 `#fde68a`
  - 中性：底色 `#f6f4ee`，描边 `#d6d0c4`；文字三级 `#1f2937`/`#4b5563`/`#7b8494`；箭头灰 `#9aa3ad`
- 图类型优先级：流程图 > 对比图 > 架构图 > 光谱/时间线；不用纯装饰图。
- 文字基线：中文文本基线比字形底高约 12%，画布底部预留 ≥ 12px 空白；图内底部注释与最近元素下边缘至少留 20px（图 8-1 曾因注释 y=268 贴着盒子底边 y=266 而重叠返工）。

## 5. 写作规律（从具体实践中蒸馏的通用法则；上限 20 条，超额即合并同类项再增加）

- **类比与科普站**：抽象概念必配一个「人手可摸」的生活化类比；补思想史 / 知识纵深用科普小站（h3 小节）。
- **对比表**：并列且需回查的易混概念做表；「正反合」中被否定的选项用散文一笔带过，不为脚手架建表。
- **「拆掉一个」实验**：拆掉某零件 → 会怎样，反衬每个零件的必要性。
- **呼应与接力（硬性）**：往前呼应、往后埋线；相邻章节必须有交接棒——上章结尾钩向下章、下章开头接住上章，跨线切换尤其需要桥。
- **哲学落到 dsh 真实结构**：抽象概念对应到真实包名 / 机制（如 llm、session、tools、agent-loop），不悬空。
- **延伸链接**：外部权威资料必须给可点击远程链接；仓库内文件一律用 GitHub 远程路径，同一文档 .zh.md 版本优先。
- **链路要走到头（硬性）**：讲注入 / 组装 / 读取类机制，必须展示「最后交给模型的是什么结构」并给带真实内容的成品示例（结构树 / 流程图不算示例）；拟人说法必须落到具体工具调用——模型出主意、工具动手；抽象概念难懂时配「走一遍」工作流。
- **先铺垫，后展开**：后续章节会详讲的内容，本章只铺垫概念 + 一句「第 X 章展开」预告，不赘述。
- **单一线索（硬性）**：每章必须有一条贯穿全章的心智模型或主线比喻，各节都挂在这条线上。
- **概念三笔线（硬性）**：跨篇概念按三笔展开——哲学篇埋概念（是什么 / 为什么 / 类比 + 预告）→ 科学篇机制章正式展开（学名、映射场景与代码）→ 回指前两笔用例。
- **术语与热词守则（硬性）**：机制术语不许突然抛裸词——首次出现给「是什么 + 一句预告」，前文埋过种子就回扣「第 X 章埋过种子的 XX」；读者提到的业界范式词当场判断归属章节，并在骨架标注锚点（学名 + 出处链接 + 回指种子）。
- **结构化优先（硬性）**：补内容先问「怎么做让最终结构化输出更合理」——融入 / 增节皆手段，判断标准是结构清晰度；多步场景与多要点叙述按「总起 · 分点 · 收束」——一行一句、按主轴排序，不许一段到底。
- **动机先行（硬性）**：讲「是什么 / 怎么工作」前先讲「解决什么问题、有什么用」——问题场景 → 目的 → 机制；写「什么时候读 / 用」这类触发条件时，同一处交代「为什么是这时候、能得到什么」，触发与动作之间不断因果。
- **四个谁（硬性）**：讲任何记忆 / 文档的读写，交代四个谁——谁写（拍板 × 动手，两者未必同一方）、谁读（使用方）、谁拍板读（触发器，一行只挂一个）、读多少（整篇照登 / 按需节选 / 子集 + 摘要）；生产 → 存放 → 消费一条线看全。
- **题文一致（硬性）**：标题与核心问题承诺的动作，正文必须有独立小节兑现；主线动作链在导语、导览、全景图、要点回顾四处一致出现。
- **实证必配实例（硬性）**：抽象法则必须落到真实文件 / 编号并给仓库内可点链接；工程化章以仓库真实约定为证，只给原则不给实物视为不具体。
- **人机分工与旋钮**：讲理念与身份的章节落到「AI 干什么、人干什么」——环节越机械越交给 AI，判断越关键越留给人；护栏立在每一环出口。讲控制 / 信任 / 权限类机制，先给「控制对象清单」（控制什么）与「旋钮清单」（人怎么控制 × 机器怎么把守），再讲机制内部；并标清「谁在判断」——模型只提案，裁决在代码（确定性规则）、拍板在人（弹窗 / 配置）。
- **回路钩子（硬性）**：应用章显式走通「提供 → 消费 → 写回」——开头点名提供方章节、结尾点名写回方章节；排序只是让章节相邻，回路靠钩子显式接上。机制与策略分开：机制能一直跑（如反复压缩），策略要设止损——长会话、多次压缩是「先交接写回、再新开会话」的信号，旧日志留档可查。
- **产品形态实证（硬性）**：讲「实际位置 / 默认值」以读者实际使用的产品形态为准，先核实再下结论——读者对实物的观察优先于仓库示例；策略与开关类机制必须交代「什么触发 + 配置在哪 + 默认值」三件套，让读者照着能找到。
- **干中学结构（硬性）**：科学篇 = 新人入职三幕——认识项目 → 使用项目 → 开发项目；每章标题用场景动词式，应用场景优先，机制章以场景开头。

## 6. HTML 技术约定

- 所有页面共用 `assets/style.css` 与 `assets/book.js`；正文不写内联 style（极个别例外）。
- 页面骨架：topbar（含 ☰）→ `main.layout`（aside.sidebar + div.container）→ prevnext → footer → sidebar-backdrop。
- 左侧目录栏按「全书概览/前言/大纲 + 三篇 + 附录」组织，**所有篇一律全展开**（details 带 open；读者要求「目录直接全展开，不用收起来」），当前章高亮；**新增/改名章节时须同步所有页面的侧栏**。
- 小节锚点：`h2 id="s1-1"`；本章导览链接到这些锚点。
- callout 三类：`note`（teal，提示/结论）、`idea`（indigo，概念/公式）、`warn`（amber，注意/边界）。
- 行内代码用 `<code>`；需要代码块时用 `<pre>`，若缺样式先补 CSS 再用。
- 表格：首行 `<th>`，内容简洁，以 ≤ 8 行为宜。

## 7. 文件与命名

- 根目录 `docs/`；三篇各一个文件夹 `part-1/ part-2/ part-3/`，附录 `appendix/`。
- 章节文件：`part-1/01-what-is-agent.html`（NN-英文短slug.html）；篇封面 `part-1/index.html`。
- 全局页：`index.html`（概览/目录）、`preface.html`（前言）、`outline.html`（大纲）。
- 占位章节完成正文后，移除「待撰写」提示与 stub 标记。

## 8. 协作流程

- **一次只写一章**，写完等读者反馈，不擅自连续推进多章。
- 反馈常见类型：深度（太浅/太深）、例子（好懂否）、图（清楚否、溢出否）、节奏（段落长短、图文比例）。
- 处理顺序：先修订本章 → 再提炼通用规则写入本文件 → 最后才写下一章。
- **反馈即资产**：读者提出的每一条意见，除了修订当前章节外，都要先判断它是「通用问题」还是「细节问题」——标准一条：删掉这条知识，其他章节的写法会不会变？会 → 通用问题 → 沉淀为 §5 规则并记入 §10 台账；不会 → 细节问题 → 只回写当前章节正文，不进宪法。只关乎本章 / 本产品具体事实的问答（如某配置在哪、默认谁判断、某命令默认放不放行）属细节问题，答案写进正文即可。
- **宪法只存规律（硬性）**：§5 只放从具体实践中蒸馏出的通用法则，具体案例与执行细节留在书籍正文、不搬进宪法；§5 清单上限 20 条、§10 台账上限 20 行——超额时先合并同类规则、把教训收进法则本体，再清零台账（无需归档）；不设「当前进度」——书籍正文本身就是进度。第 1 次清零：2026-07-24（98 行台账归纳进 §5 后清空）；同日第 1 次蒸馏：§5 剥掉案例细节、只留 20 条通用法则。
- **已放弃的做法（防回退，别走回头路）**：不设「当前进度」（正文即进度）· 宪法不重复大纲（章序以 outline.html 为准）· 具体案例与执行细节不进宪法（只存规律）· 不单设「双线地图」卡（靠章徽章与正文自然表达）· 常见疑问不用「一问一答」小节（织进正文叙述）· 不画蛇添足（可有可无的括注与自我夸赞式铺垫直接删）。

## 9. 发布前自检清单

- [ ] 浏览器打开无渲染异常；移动端侧栏抽屉可用
- [ ] 所有 SVG：元素不溢出 viewBox/面板；marker id 无重复；底部留白足够
- [ ] 图编号连续且 caption 齐全；锚点可跳转；本章导览与正文小节一致
- [ ] 无大段纯文字小节（每小节至少 1 个非文字元素）
- [ ] 每章六件套齐全：导语 + 核心问题、本章导览、正文、一张图总结、要点回顾、动手做
- [ ] 章节链接与首页/大纲/侧栏一一对应；无死链
- [ ] 中文标点（「」）与中英空格

## 10. 反馈台账（意见 → 规则 映射；上限 20 行，超额归纳进 §5 后清零）

> 2026-07-24 清零：此前 98 行台账已全部归纳进 §1–§9 与 §5 规则本体，本表从零开始。

| 读者反馈 | 沉淀为 |
|---|---|
| 「定位与主线是不是可以浓缩一下，感觉目前的内容和书籍中的大纲有点雷同」 | §1 由 15 条浓缩为 8 条：删三篇章号范围与双线罗列细节（大纲已承载），写入「具体章序以 outline.html 大纲为准（宪法不重复大纲）」；顺带修复陈旧章号（旧「科学篇 7–16 / 第三篇 17–23」与重排后的 7–18 / 19–25 不符，删后不再有此隐患）；判定口诀、三篇分工、章号引用规则等原则全部保留 |
| 「『记忆表三件事』这汇总很具体的就不应该放在 AGENTS.md 内，应该放总结的一些规律，具体的事情做完就好了，从中总结出来的通用规律可以放 AGENTS.md 内」 | §5 第 1 次「蒸馏」：20 条全部剥掉具体案例与执行细节（第 10 章表格单元格、agent/pre-step 案例、9.1 JSONL 案例、三条边界清单等），只留通用法则；「记忆表三件事」提炼为「四个谁」（谁写=拍板×动手 · 谁读 · 谁拍板读 · 读多少）；§5 标题改「写作规律（通用法则）」；§8 规则改「宪法只存规律」 |
| 「现在我们也做了那么多 Agent 相关的学习，感觉可以优化一下我们的 AGENTS.md，你觉得还有哪些优化点呢？」（提 5 个点，采纳 3） | 文件头 blockquote 扩为四段：读法说明（谁读=每次注入给写作 Agent，这是本书自己的 AGENTS.md；何时读=动笔前对照 §1–§9、改完写 §5/§10）+ 核心问题（怎么写一本既深又浅的书——机制讲透、读者接得住）+ 宪法口诀（正文存事实，宪法存规律；规律不过二十，台账不超二十；做完一章，再走下一章）；§8 补「已放弃的做法（防回退）」清单六条（不设进度 / 不重复大纲 / 不搬案例 / 不设双线地图卡 / 不用问答小节 / 不画蛇添足）；未采纳两案：自检清单加瘦身项、每条规律补「为什么」（避免宪法变长） |
| 「对于一个 128 轮 · 737 步、压缩过多次上下文的 agent 对话，是应该新开一个会话比较好，还是继续压缩上下文呢？」 | 答：先交接写回、再新开（压缩是机制、换会话是策略；对话是现场、文件是培训；旧日志留档可查；只差临门一脚才再压一次干完）；10.2 补「压缩还是新开会话：三个信号」段（高汤第二锅 / 主线还长 / 人看不清现场——三条占两条即先交接再新开）+ 要点回顾补第 5 条「压缩到第二锅就换锅」；§5 回路钩子补「机制与策略分开，设止损」句 |
| 「写第 11 章《技能是这样被使用的》」 | 第 11 章成稿（3 图 1 表 1 pre；应用线；主线「锦囊」）：11.1 技能是什么（工具 vs 技能对比 + 锦囊比喻）、11.2 技能库（11 个技能全量清单 + 真实 SKILL.md 纸条/正文，链接 dsh 仓库）、11.3 调用链路（三触发通道 + 四步链路图 + 真实走一遍 + 四个谁 callout）、11.4 拆开之后（guidance not a script 原话 + 锦囊互援 + 验收 + 双线提示 + 回路钩子）；开头接第 10 章「什么时候想起、怎么调进来」钩子，结尾递第 12 章钥匙；大纲状态同步已完成；自检通过（无 stub、图 3 + caption 3、marker 唯一、锚点 4、六件套齐） |
| 「skills 应该还可以执行脚本是吗？这块文档中没有讲解」 | 核实：record-browser-gif 自带 scripts/encode_gif.py（指导书称「bundled encoder」），由模型用 bash 工具执行；技能机制本身只加载 SKILL.md 文本、不自动执行任何东西；11.4 补段（脚本是锦囊里的工具物料，执行者永远是模型 + 工具，链接真实目录）+ 11.2 清单项补注 + 要点回顾第 5 条同步；规则：读者问「还能不能 X」，先核实仓库再答，并回写正文 |
| 「skill 和 tool 的关系是什么？MCP 又是什么？感觉职责有交叉，是我的感觉对吗？/ 文章只写了如何应用 skills，没讲 Agent 怎么实现读取与调用」 | 答：skill=内容（手册）、tool=手（动作）、skill 工具=两者之间的门——交叉感对了一半，交叉点就是 skill 工具（既像 read 被调用、又是技能系统入口）；MCP=行业标准外部能力接缝（dsh 有 packages/mcp/mcp-client 早期形态），归属第 17 章；体内实现（ctx.skills 注册 + skill-filesystem 发现 + tool-skill 目录/loader）归属第 14 章；11.1 补三层关系段、11.3 双线提示补 MCP 预告、ch14/ch17 骨架挂写正文备忘 |
| 「『技能给的是指导不是脚本』——但 skill 内写了执行命令（如 git status），agent 会去执行对应指令吗？」 | 答：会——命令确实执行（模型叫 bash 工具真跑）；「不是脚本」指「不会自动跑」，每步执行前模型都做判断；11.4 开头段改写为「命令会跑，判断在模型」（先澄清再解释 guidance not a script 原意），要点回顾第 5 条同步；规则：易读岔的表述要「先澄清误解、再给正解」 |
| 「也就是说 skill 和文档最大的区别，就是 skill 会执行某些行为，而不是只补充记忆？」 | 修正精度：机制上两者一样（都是文本进上下文，谁都不执行，执行者是模型 + 工具）；区别在内容形态——文档=地图（是什么/为什么，补认知）、技能=行军命令（怎么做/什么算合格，给剧本）；11.1 补「文档是地图，技能是行军命令」段 + 钉句升级为「工具教手怎么动，技能教活怎么干，文档告诉你世界什么样」；要点回顾第 1 条改「手 / 剧本 / 地图」 |
| 「我最主要的疑惑是为什么需要 skills，而不是用 AGENTS.md 来承载？skills 有什么不可替代的理由？」 | 答：读法不同——AGENTS.md 每次必读（原文照登），技能按需调用；若 11 本指导书全进 AGENTS.md，每轮照登=预算白花、信号淹没；技能不可替代点=两级结构（纸条常驻菜单 + 正文按需加载），AGENTS.md 只能整篇照登、没有目录层；11.1 开头补「为什么不用 AGENTS.md 承载」段 + AGENTS.md vs 技能四行对比表（装什么/何时读/换着放的代价/类比）+ 要点回顾第 2 条改「为什么单开技能库」；规则：讲「为什么要有 X」先答「与最近似替代品的读法差异」 |
| 「11.4 拆开之后：照做，也要盯也是一大段输出，可读性差。重新整理整个 11 章的内容，结构化输出」 | 整章结构化重构：11.1 去两段 FAQ 散文与重复表，改「三身份一张表」（工具=手/技能=剧本/文档=地图 四行）+「门」三层 ul（技能是内容/工具是手/skill 工具是门）+ 一句话收束；lead 来历三句拆 ul；「为什么」段拆两段；11.4 改「总起一句（命令会跑判断在模型）+ 三个事实分点 ul（命令真跑/脚本只是物料/锦囊互援）+ 验收三件事 ul」；双线提示三件事拆列表（体内实现→14 / 写技能→20 / MCP→17）；终态 2 表 3 图 7 ul，无长段 |
| 「图 11-3 文字和图重叠了（×3）」 | 图 11-3 底部注释 y=314 距最后一盒下边缘（308）仅 6px，违反 §4「底部注释与最近元素 ≥20px」；viewBox 330→354、注释 y 314→338（间距 30px、底留白 16px）；与图 8-1 同病，§4 规则已有，此为执行漏检——写完图必逐图核对底部注释间距 |
| 「写第 12 章《什么时候把钥匙交给它》」 | 第 12 章成稿（4 图 2 表 6 callout；应用线；主线「管家与钥匙」）：12.1 信任分档三把钥匙（全信/核查/亲为 + 三问定档）、12.2 审查工作流（三时点：查计划 plan mode 软引导硬执行 / 查执行四道关卡 / 查产物复盘三问）、12.3 升降级（出错降档 · 连赢升档 · 教训固化成护栏）；开头接第 11 章「哪些环节该点头」钩子与第 8、6 章种子，结尾递第 13 章第三幕开场；实证：pre-execute → ctx.approval（无人应答即否决）→ tools.guard() 单调守卫（只否决无放行）→ sandbox-local 三档（read-only/workspace-write/danger-full-access + 升权走批准通道），全部核实 dsh 源码并给 GitHub 链接；大纲状态同步已完成；自检通过（图 4 + caption 4、marker m121–m124 唯一、锚点 3、六件套齐） |
| 「感觉不太明白什么需要控制，怎么控制呢？」 | 12.1 补「控制的对象是动作，不是态度」+「拿四件日常活试三问」对照 ul（重命名全信 / 批量改核查 / 删文件核查 / 发布亲为）；12.2 补分工钉句 callout「机器管每一刀，你管抽查与拍板」+ 真实走一遍（删工作区外文件：沙箱拦 → 升权提示 → 批准弹窗 → allowed-once → approval/asked·decided 审计留痕，全部核实 escalation.ts 原话）+「六个旋钮」表（/plan · 批准弹窗 · 文件策略 · 日志复盘 · AGENTS.md · postmortem/lefthook/knip）；图 12-2 标题与盒注标「你的 / 机器的」分工、图 12-4 主线句、要点回顾第 2 条、动手做第 2 步（改「故意越界」实验）同步；§5「人机分工视角」扩为「人机分工与旋钮」——控制类机制先给对象清单与旋钮清单，再讲机制内部 |
| 「那怎么样的行为是会触发批准吗？是有黑白名单之类的配置吗？在哪呢？」 / 「git 有很多命令，默认有些能用有些不能用，谁来判断？」 / 「那个判断是 agent 代码还是大模型决定的？」 | 三条均为 dsh 细节问答：答案只回写 12.2 正文（触发批准三类行为、黑白名单三层配置、沙箱按文件效果判·命令级规则自配、模型提案·代码裁决·人拍板五角色表），不进台账细节；从中共蒸馏出两条通用规律进 §5——「产品形态实证」补「什么触发 + 配置在哪 + 默认值」三件套、「人机分工与旋钮」补「谁在判断」（模型只提案，裁决在代码、拍板在人）。细则问答不再单列台账（见 §8 判断标准） |
| 「这篇文章要结构性重新输出：先讲完全放开的危害，再讲要防止危害需达到的标准，最后讲 agent 是怎么做到的」 | 第 12 章按「危害 → 标准 → 机制」三步重排（通用结构原则，兑现 §5「动机先行」——问题场景 → 目的 → 机制）：新增 12.1 危害节（「拆掉一个」实验：拆哪道把关放开哪种危害表 + 为什么不能靠模型自觉）；原三把钥匙并入 12.2 标准节（危害→标准镜像表 + 三问定档）；原审查/升降级内容组装进 12.3 机制节（分工口诀 → 谁在判断 → 四道关卡 → 三时点 → 走一遍 → 旋钮 → 升降级固化）；图 12-4 全景改为「危害→标准→机制」三盒；大纲 12.1–12.3 结构清单同步；标题暂不改（作者决定保留原名） |
| 「agent 会给用户只读模式、工作区模式、自动模式等，这些模式的功能和机制差异是什么？」 | 细节问答，答案回写 12.3 正文：三种沙箱模式拆开看（read-only 只读 / workspace-write 工作区 / danger-full-access「Full access」，无「自动模式」此名，其「自动感」来自配套批准策略 never 无确认弹窗）；关键心智模型——前两档是「墙」（内核强制、写的那一刻拦截），第三档是「拆墙」（bypass，等于没有沙箱）；判定维度只有文件效果（网络/进程可见性不在词汇表）；升权阶梯只升不降、带理由一次一问、执行前裁决；换挡写日志、会话创建时固定；12.3 关卡表后补三模式表 + 三要点段 |

