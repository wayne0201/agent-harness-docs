# 《深入浅出 AI Agent》写作规范

> 本书的「写作宪法」。每次会话自动注入给写作 Agent——这是本书项目自己的 AGENTS.md。
> **核心问题**：怎么写出一本既深又浅的书——机制讲透、读者接得住？
> **宪法口诀**：正文存事实，宪法存规律；规律不过二十；做完一章，再走下一章。

## 1. 定位与主线

- 书名：《深入浅出 AI Agent》；双标本：DeepSeek Harness（`dsh`）与 Codex（`codex`）。读者：有编程基础、不要求懂任何框架；重「心智模型」，轻 API 手册。章序以侧栏目录为准（宪法不重复大纲）。
- **主线**：哲学 → 科学 → 艺术——先问为什么 → 建立机制（解剖双标本的通用规律与实现差异）→ 长出品。
- **主干与枝干（硬性）**：通用哲学是每章主干（主要内容），dsh 与 codex 的设计哲学是从主干延伸出的枝干（辅助对照）。判断标准：删掉枝干，主干必须仍然成立、仍然完整。
- **全书心智模型**（第 1 章确立，反复回扣）：**Agent = 模型（思考）+ 记忆（上下文）+ 工具（行动），由循环（Loop）驱动**。
- **暗线**：全链路工程师（发现 → 梳理 → 落地 → 反馈 的闭环）。
- **双线结构（硬性）**：应用线（用好 Agent）/ 创造线（构建 Agent）。判定口诀：改 Agent「体内」→ 创造线；改「相处」→ 应用线。编织法：主题成对相邻；应用章结尾埋「做」的钩子，创造章回看「用」的体验。
- **章号引用规则（重编号时必查）**：指向「篇」的引用不变；篇内局部章号与图表裸数字引用必须随重编号同步。
- **三篇分工（硬性）**：哲学篇只讲概念与领域知识，不深入机制与源码；机制细节后移到科学篇。

## 2. 每章统一体例

按序组织一章（六件套）：
1. **导语**：hero（章号 + 标题 + 副题）+ lead 段 + 「本章核心问题」callout（idea）。
2. **本章导览**：card 内的小节锚点链接列表。
3. **正文小节**：h2 编号（1.1、1.2…），短段落 + 图/表/列表/callout 组合。
4. **一张图总结**：把全章脉络收拢成一张图。
5. **要点回顾**：3–5 条，每条一句话。
6. **动手做**：不依赖代码的小练习，1–3 步，写在 card 里。

每章（含 stub）保留三段元信息：**核心问题 / 本章结构 / 你将获得**。
- 不单设「双线地图」卡；只有跨界知识点才挂**双线提示** callout（标题「⇄ 双线提示」，写明另一条线视角）。
- 全章口诀句用 callout（note + 短标题）高亮承载，不埋在段落里只加粗。

## 3. 可读性要求（最高优先级）

- **段落短**：1–3 句，禁止大段堆砌；每小节至少一个非纯文字元素。
- 关键概念首次出现加粗（`<b>`）；一屏内重点句加粗不超过两三处。
- 中文引号用「」；中英文之间留空格。
- **结构感优先**：列表、表格、callout 优于长句；能用一句话讲清的不写一段。
- **中文表述优先**：避免直译词，用动词式·生活化标题（曾改《好项目是「AI 友好」的项目》→《让项目会「自我介绍」》）。
- **并列实体必分点（硬性）**：一段并列三个以上实体必须拆列表/表格。
- **疑问融入正文（硬性）**：常见疑问不要用「一问一答」小节，把答案织进叙述。
- **写作从简（硬性）**：可有可无的括注、自我夸赞式铺垫直接删。

## 4. 插图规范（内联 SVG）

- 只用**内联 SVG**，不依赖外部资源，离线可看。
- 图编号：图 章-序（图 1-1、1-2…）；子图：图 1-2a。每图必配 figcaption。
- **箭头必有落点（硬性）**：带 marker 的 line/path 终点必须落在元素内；删目标盒同步删/改指箭头。
- **边界检查（硬性）**：所有 rect/text 必须落在 viewBox 与面板边界内；写完必查最大/最小坐标。
- **元素组居中（硬性）**：面板内元素组中心与面板中心对齐；写完必算组中心。
- **marker id 全页唯一**：SVG 的 id 是文档全局的，重复会互相覆盖箭头颜色。
- SVG 内 `<g>` 统一加 `font-family='-apple-system,"PingFang SC","Microsoft YaHei",sans-serif'`。
- 配色：哲学/模型=靛蓝 `#4338ca`/`#eef2ff`/`#c7d2fe`；科学/循环=青绿 `#0f766e`/`#ecfdf8`/`#5eead4`；艺术/行动=琥珀 `#b45309`/`#fffbeb`/`#fde68a`；中性 `#f6f4ee`/`#d6d0c4`，文字三级 `#1f2937`/`#4b5563`/`#7b8494`，箭头灰 `#9aa3ad`。
- 图类型优先级：流程图 > 对比图 > 架构图 > 光谱/时间线；不用纯装饰图。
- 文字基线：中文文本基线比字形底高约 12%，画布底部预留 ≥12px；图内底部注释与最近元素下边缘 ≥20px。

## 5. 写作规律（基本准则；合并同类，不无限膨胀）

- **类比与科普站**：抽象概念必配生活化类比；知识纵深用科普小站（h3）。
- **对比表**：易混概念做表；被否定的选项散文一笔带过，不为脚手架建表。
- **「拆掉一个」实验**：拆掉某零件 → 会怎样，反衬必要性。
- **呼应与接力（硬性）**：上章结尾钩向下章、下章开头接住上章；跨线切换尤其要桥。
- **先通用，后实例，再亮点（硬性）**：讲机制先给「所有同类实现都一样的通用骨架」（与框架无关），再落「dsh 怎么实现」（通用概念在此得到具体名字），最后才点「dsh 的特别设计」——没有就直说，不硬凑。禁止一上来就扎进 dsh 内部命名。
- **单一线索（硬性）**：每章一条贯穿全章的心智模型/主线比喻，各节都挂在这条线上。
- **概念三笔线（硬性）**：跨篇概念按「哲学埋概念 → 科学篇机制展开（学名/代码）→ 回指用例」三笔展开。
- **术语守则（硬性）**：机制术语不许突然抛裸词——首次出现给「是什么 + 一句预告」；前文埋过种子就回扣。
- **结构化优先（硬性）**：多步/多要点按「总起 · 分点 · 收束」，一行一句、按主轴排序，不许一段到底。
- **动机先行（硬性）**：讲「是什么/怎么工作」前先讲「解决什么问题」；触发条件与动作之间不断因果。
- **四个谁（硬性）**：讲记忆/文档读写，交代谁写（拍板×动手）、谁读、谁拍板读、读多少；生产→存放→消费一条线看全。
- **题文一致（硬性）**：标题与核心问题承诺的动作，正文必须有独立小节兑现；主线动作链在导语/导览/全景图/要点回顾四处一致。
- **实证必配实例（硬性）**：抽象法则必须落到真实文件/编号并给仓库内可点链接（GitHub 远程路径，.zh.md 优先）。
- **链路要走到头（硬性）**：讲注入/组装/读取，必须展示「最后交给模型的是什么结构」+ 带真实内容的成品示例；拟人说法落到具体工具调用。
- **人机分工与旋钮**：讲理念/身份落到「AI 干什么、人干什么」；讲控制/权限先给「控制对象清单 + 旋钮清单」，再讲机制；标清「谁在判断」——模型只提案，裁决在代码，拍板在人。
- **回路钩子（硬性）**：应用章显式走通「提供 → 消费 → 写回」——开头点名提供方、结尾点名写回方；机制与策略分开、设止损。
- **产品形态实证（硬性）**：讲「实际位置/默认值」以读者实际使用的产品形态为准，先核实再下结论；策略/开关必须交代「什么触发 + 配置在哪 + 默认值」三件套。
- **干中学结构（硬性）**：科学篇 = 新人入职三幕（认识 → 使用 → 开发）；标题用场景动词式，机制章以场景开头。

## 6. HTML 技术约定

- 所有页面共用 `assets/style.css` 与 `assets/book.js`；正文不写内联 style。
- 骨架：topbar（含 ☰）→ main.layout（aside.sidebar + div.container）→ prevnext → footer → sidebar-backdrop。
- 侧栏按「全书概览 + 三篇 + 附录」组织，**所有篇全展开**（details 带 open），当前章高亮；**新增/改名章节时同步所有页面侧栏**。
- 小节锚点：`h2 id="s1-1"`；导览链接到锚点。
- callout：`note`（teal）/ `idea`（indigo）/ `warn`（amber）。
- 行内代码 `<code>`；代码块 `<pre>`。表格首行 `<th>`，≤ 8 行。

## 7. 文件与命名

- 根目录 `docs/`；三篇 `part-1/ part-2/ part-3/`，附录 `appendix/`。
- 章节文件：`NN-英文短slug.html`（如 `01-what-is-agent.html`）；篇封面 `index.html`。
- 全局页：`index.html`（首页 = 全书概览 + 叙事，单页入口）。
- 首页承担序言（为什么写、主干与枝干、怎么读）；完整章目在侧栏目录，不再单设序/大纲页。
- 占位章节完成正文后，移除「待撰写」提示与 stub 标记。

## 8. 协作流程

- **一次只写一章**，写完等读者反馈，不擅自连续推进多章。
- 处理顺序：先修订本章 → 提炼通用规则并入 §5 → 再写下一章。
- **即改即炼（硬性）**：读者每一条意见，先判断「通用问题」还是「细节问题」——删掉这条知识，其他章节写法会不会变？会 → 立即提炼并入 §5 相应规律（合并同类，不无限膨胀）；不会 → 只回写当前章节正文。**不设反馈台账**——问题当下解决、当下沉淀。
- **宪法只存规律（硬性）**：§5 只放通用法则，具体案例与执行细节留在正文；§5 上限 20 条，超额先合并再增。
- **已放弃的做法（防回退）**：不设「当前进度」· 不重复大纲 · 案例不进宪法 · 不单设双线地图卡 · 不用问答小节 · 不画蛇添足。

## 9. 发布前自检清单

- [ ] 浏览器打开无渲染异常；移动端侧栏抽屉可用
- [ ] 所有 SVG：元素不溢出 viewBox/面板；marker id 无重复；底部留白足够
- [ ] 图编号连续且 caption 齐全；锚点可跳转；导览与正文小节一致
- [ ] 无大段纯文字小节（每小节至少 1 个非文字元素）
- [ ] 每章六件套齐全
- [ ] 章节链接与首页/侧栏一一对应；无死链
- [ ] 中文标点（「」）与中英空格
